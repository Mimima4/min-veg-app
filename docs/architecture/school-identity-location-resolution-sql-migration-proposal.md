# School Identity / Location Resolution — SQL/Migration Proposal

## 1. Status / boundary

- Status: **PROPOSAL / NOT APPROVED**
- Nationwide model intent.
- No SQL execution in this proposal.
- No migration file is created by this proposal.
- No schema/code/write integration is approved by this proposal.

### Main disclaimer

This is a migration proposal only:

- it does not create migration files;
- it does not execute SQL;
- it does not approve schema changes;
- it does not approve code/runtime/write integration.

### Non-executable format safeguard

- No executable SQL blocks are included.
- No SQL-like DDL snippets (`CREATE TABLE`, `ALTER TABLE`, `CREATE INDEX`) are included.
- This document uses structured prose and tables only.
- This proposal must not be copy-paste runnable as a migration.

## 2. Normative sources / precedence

Primary references:

- `docs/architecture/school-identity-location-resolution-schema-design-draft.md`
- `docs/architecture/school-identity-location-resolution-db-model-proposal.md`
- `docs/architecture/school-identity-location-resolution-phase-2-spec.md`
- `docs/architecture/norway-school-identity-matching-spec.md`
- `docs/architecture/route-engine-master-spec.md`
- `docs/product-principles.md`

Conflict policy:

- If conflict exists, mark: **UNRESOLVED ARCHITECTURE CONFLICT**.
- Do not resolve conflict silently inside this proposal.
- No schema design approval may proceed until a separate architecture decision record resolves conflict.

## 3. Proposed migration units (conceptual only)

No SQL is provided; units are proposal-level sequencing:

1. create `source_school_observations`
2. create `school_identity_candidates`
3. create `identity_aliases`
4. create `school_locations`
5. create `school_identity_resolution_decisions`
6. create `programme_availability_publication_decisions`
7. create `school_identity_review_events`
8. add supporting indexes/constraints (conceptual)
9. add RLS/security policies if applicable (conceptual, security review required)
10. no backfill execution in this step unless separately approved

## 4. Physical enum/check strategy proposal

Proposal only (not final decision):

- Prefer **constrained text + check constraints** as initial physical strategy.
- Keep native database enum as a considered alternative, not accepted yet.

Tradeoff:

- constrained text + checks: easier evolution and deprecation management;
- native enums: stronger DB-level typing, but less flexible evolution.

Boundary:

- final enum strategy requires explicit SQL/migration proposal acceptance.
- enum changes must never silently change runtime behavior.

## 5. Table-by-table SQL-shape proposal (non-executable)

### 5.1 `source_school_observations`

- Likely column categories: `uuid`, `text`, `timestamptz`, `jsonb`.
- Nullability: source identity/snapshot/profession/stage and observed timestamp required.
- PK concept: single surrogate identifier.
- FK concept: parent for candidate/decision tables.
- Unique concept: dedupe on source snapshot + source school identity context.
- Index concepts: by snapshot, county/profession/stage.
- Check concepts: non-empty source identifiers.
- Mutability: append-only.
- JSON strategy: optional observation payload with minimum shape contract.
- Security/RLS: read access scoped to service/operator classes (policy-level only).
- Forbidden contents: final publishability/resolution decisions.

### 5.2 `school_identity_candidates`

- Likely categories: `uuid`, `text`, `jsonb`, `timestamptz`.
- Nullability: observation reference, identity candidate key, signal version required.
- PK concept: surrogate row id.
- FK concept: to source observation.
- Unique concept: dedupe per observation/candidate/version.
- Index concepts: observation lookup, candidate identity lookup.
- Check concepts: non-empty candidate key and signal version.
- Mutability: append-only by generation.
- JSON strategy: structured `match_signals` contract required.
- Security/RLS: generated by system boundary; operator read-only in normal mode.
- Forbidden contents: direct publication approval.

### 5.3 `identity_aliases`

- Categories: `uuid`, `text`, `timestamptz`, `jsonb`.
- Nullability: identity key, alias label/type, evidence reference required.
- PK concept: surrogate id.
- FK concept: conceptual link to identity model key.
- Unique concept: normalized alias uniqueness per identity/type/language.
- Index concepts: by identity key and normalized alias.
- Check concepts: alias type must be approved enum-like value.
- Mutability: append with validity intervals/supersession.
- JSON strategy: evidence references with minimal structure.
- Security/RLS: service/operator only.
- Forbidden contents: runtime route publish flags.

### 5.4 `school_locations`

- Categories: `uuid`, `text`, `jsonb`, `timestamptz`.
- Nullability: location key and identity key required.
- PK concept: surrogate id.
- FK concept: conceptual identity + optional NSR institution link.
- Unique concept: identity/location uniqueness; optional identity+institution uniqueness.
- Index concepts: identity key, county/municipality, NSR institution.
- Check concepts: stable key format and evidence presence.
- Mutability: versioned/superseded, no destructive overwrite.
- JSON strategy: location evidence references.
- Security/RLS: service/operator scope.
- Forbidden contents: implicit publication states.

### 5.5 `school_identity_resolution_decisions`

- Categories: `uuid`, `text`, `jsonb`, `timestamptz`, `boolean` (optional active marker alternative not preferred).
- Nullability: decision state, actor type, basis version, reason codes, audit ref, decided timestamp required.
- PK concept: surrogate id.
- FK concept: observation id + conceptual links to identity/location/provider entities.
- Unique concept: one active decision per observation.
- Partial unique concept: active-row uniqueness via supersession marker (see section 6).
- Index concepts: active decisions, unresolved backlog, stale review queue.
- Check concepts: publish-oriented rows require audit+basis fields.
- Mutability: append-only with supersession.
- JSON strategy: reason/evidence payload contract required.
- Security/RLS: system + authorized operator actions.
- Forbidden contents: destructive overwrite; hidden manual overrides.

### 5.6 `programme_availability_publication_decisions`

- Categories: `uuid`, `text`, `jsonb`, `timestamptz`.
- Nullability: observation/programme/stage, publishability state, audit+basis fields required.
- PK concept: surrogate id.
- FK concept: to observation + conceptual linkage to active resolution decision.
- Unique concept: one active publication decision per observation/programme/stage.
- Partial unique concept: active-row uniqueness via supersession marker.
- Index concepts: publication-ready set, blocked-review set, by county/profession/stage through joins.
- Check concepts: publication gate prerequisites represented in state/field consistency checks.
- Mutability: append-only with supersession.
- JSON strategy: decision reason/evidence contract.
- Security/RLS: no public/family read; service/operator boundary.
- Forbidden contents: publishability without audit_ref + decision_basis_version.

### 5.7 `school_identity_review_events`

- Categories: `uuid`, `text`, `jsonb`, `timestamptz`.
- Nullability: target decision id, event type, actor id/type, timestamp required.
- PK concept: surrogate id.
- FK concept: target decision reference.
- Unique concept: event stream by id.
- Index concepts: backlog by event type/age, actor workload, unresolved queue.
- Check concepts: event type in approved set; payload must include reason/evidence linkage.
- Mutability: append-only.
- JSON strategy: event payload schema contract.
- Security/RLS: operator/service scope with strict write authority.
- Forbidden contents: replacing authoritative decision row.

## 6. Active-row uniqueness strategy

Canonical proposed mechanism:

- append-only rows with **`superseded_at IS NULL`** as active-row marker.

Rules:

- one active resolution decision per observation;
- one active publication decision per observation/programme/stage;
- supersession inserts new row and closes prior row with superseded marker;
- append-only history preserved.

Alternative:

- `is_active_current` is treated as a rejected alternative in this proposal unless separately approved.

Boundary:

- exact physical constraint implementation remains subject to SQL review.

## 7. JSON validation contracts

Minimum JSON shape contracts (proposal-level):

- `decision_reason_codes`: non-empty array of approved reason-code text.
- `match_signals`: object with signal keys + version metadata.
- `evidence_refs`: array/object of source references.
- `audit_payload`: object containing actor, before/after state, reason codes, evidence refs, snapshot, basis version, timestamp.
- `event_payload`: object scoped to review event with reason/evidence references.

Rules:

- malformed payloads must not qualify for publishable decision path.
- JSON contract versioning should be explicit in SQL/migration phase.

## 8. RLS/security and operator authority (policy-level)

Policy-level only (not final rules):

- define read boundaries for service/operator/system roles;
- system boundary can insert generated candidate rows;
- operator boundary can create review events and audited decision transitions;
- publishable approvals require audited authority;
- unresolved/review datasets are not public/family-facing.

Boundary:

- final RLS rules require separate security review.
- do not assume final auth/role topology beyond conceptual service/operator/system boundaries.

## 9. Stale severity / revalidation triggers

Hard-stale (blocks publication and requires revalidation):

- school code change;
- programme/stage availability change;
- source snapshot lineage drift;
- NSR institution active/private/location changes affecting semantics;
- alias/location evidence drift invalidating active basis.

Soft-stale (advisory/review signal):

- label/display variants with stable identity evidence;
- non-material metadata drift.

Interaction:

- `source_snapshot_label`, `signal_version`, and `decision_basis_version` must remain consistent with active decision.
- hard-stale requires supersession before publication.

## 10. Key collision and merge/split governance

- identity_key collision handling must be deterministic and audited.
- location_key collision handling must be deterministic and audited.
- identity merge flow must preserve lineage via supersession/mapping.
- identity split flow must preserve lineage via supersession/mapping.
- no destructive key reuse.
- no display-label-only key derivation.

## 11. Retention / archival and backfill sequencing

Retention/archival proposal:

- append-only history retained with supersession chain.
- no destructive delete while referenced by decision/audit rows.
- retention minimum is policy-owned (compliance owner).
- archival must preserve decision/audit chain.

Backfill boundary:

- no backfill execution approved here.
- any backfill algorithm requires separate ADR/plan and approval.
- this document defines sequencing principles only.

Rollback sequencing (proposal-level):

- rollback strategy must preserve audit/decision history and avoid silent truth loss.

## 12. Publication safety checks (pre-PSA integration)

Before any PSA write-path integration:

- active publication decision exists;
- `publishability_state = publishable`;
- active identity resolution decision exists;
- decision is not stale;
- required audit fields are present;
- state is not `unsupported_losa` / `external_delivery`;
- provider/location semantics are resolved.

## 13. Rollout plan

1. SQL/migration proposal review.
2. Dry-run migration plan design.
3. Apply migrations only after explicit approval.
4. Read-only simulation.
5. Operator review workflow.
6. Controlled publication.
7. Rollback plan verification.
8. No write integration until separate approval.

## 14. Risks / mitigations

- DB complexity -> phased rollout + narrow first scope.
- performance/index risk -> query-profile-driven index validation.
- RLS misconfiguration -> dedicated security review gate.
- review backlog -> governance ownership + ageing policy.
- stale decision leakage -> hard-stale blocking rules.
- accidental publishability expansion -> strict publication gate checks.
- enum evolution drift -> explicit versioning/deprecation policy.
- bad backfill -> separate approved ADR + dry-run validation.

## 15. Acceptance criteria for SQL/migration proposal

- reviewed and accepted by architecture gate;
- no conflicts with locked specs;
- resolves schema draft acceptance notes;
- contains no executable SQL snippets;
- contains no runtime/code/write integration approval;
- migration file creation remains blocked until proposal approval.

## 16. Explicit non-goals

- no SQL execution now;
- no migration file now;
- no data backfill now;
- no Route Engine runtime changes;
- no readiness JSON changes;
- no PSA write changes;
- no UI/admin dashboard changes.

## 17. Acceptance status

- Status: **ACCEPTED WITH NOTES**
- No blocking conflicts found with:
  - `docs/architecture/school-identity-location-resolution-phase-2-spec.md`
  - `docs/architecture/school-identity-location-resolution-db-model-proposal.md`
  - `docs/architecture/school-identity-location-resolution-schema-design-draft.md`
  - `docs/architecture/norway-school-identity-matching-spec.md`
  - `docs/architecture/route-engine-master-spec.md`
  - `docs/product-principles.md`
  - Phase 2 gates in `docs/architecture/norway-school-identity-matching-execution-plan.md`
- This proposal is the accepted baseline for the next **migration file draft** step.
- No migration execution is approved by this proposal.
- No DB schema change is approved by this proposal.
- No code/runtime/write integration is approved by this proposal.

Notes to carry into migration file draft:

1. Finalize exact SQL types and FK forms.
2. Formalize JSON contract enforcement/versioning.
3. Implement physical partial unique constraints for active rows.
4. Require separate RLS/security review before applying migrations.
5. Define concrete rollback/no-data-loss plan per migration unit.
6. Define dry-run migration validation plan.
7. Keep backfill excluded until separate ADR/approval.

## 18. Migration draft acceptance status

- Status: **APPROVE WITH CONDITIONS**
- Reviewed migration draft:
  - `supabase/migrations/20260506112154_school_identity_location_resolution_phase2.sql`
- No hard SQL blockers found for migration draft candidate status.
- Migration execution is **NOT approved** by this status.
- Apply remains blocked until all pre-apply conditions are satisfied.

Required pre-apply conditions:

1. Separate security/RLS review and approved policy/grants model.
2. Supabase `pgcrypto` extension policy/permissions check.
3. Dry-run migration plan review.
4. Apply remains strictly additive-only.
5. No backfill.
6. No existing runtime/truth table changes.
7. No runtime/write integration.

Next gate:

- **dry-run migration plan**

## 19. Security/RLS review status

- Status: **APPROVED FOR ADDITIVE TABLE MIGRATION WITH CONDITIONS**
- Migration execution is **NOT approved** by this status.
- Apply remains blocked until all Security/RLS conditions and dry-run migration plan approval are complete.

Required conditions before apply:

1. Approve explicit security/RLS role-policy matrix for:
   - `service/system`
   - `admin/operator`
   - `anonymous/public`
   - `authenticated family user`
   - `school user`
   - `kommune/fylke institutional user`
   - `future mobile/API client`
2. Enforce service-role-only access to Phase 2 tables at initial apply.
3. Define audited operator transition rules before operator workflow.
4. Validate Supabase `pgcrypto` extension/permissions policy.
5. Keep runtime/client paths pinned to published truth only.
6. No public/family/mobile direct access to Phase 2 internal tables.
7. No hidden manual truth.

Next gate:

- **dry-run migration plan**

## 20. Dry-run migration plan

Status:

- Dry-run migration plan is documented and ready for execution review.
- Migration execution remains **NOT approved** by this plan.

Goals:

1. Prove migration is additive-only.
2. Prove no existing runtime/truth table changes.
3. Prove no data writes/backfill.
4. Prove dependency order is valid.
5. Prove rollback order is valid.
6. Confirm `pgcrypto` policy/permissions handling.
7. Prove no RLS/policy/grant side effects.
8. Prove no runtime integration.

Static checks:

1. No `ALTER TABLE` on existing tables.
2. No `INSERT`/`UPDATE`/`DELETE`.
3. No `CREATE POLICY`/`GRANT`/`REVOKE`.
4. No `CREATE TRIGGER`/`CREATE FUNCTION`.
5. No county/fylke-specific logic.
6. Object names are `<63` chars.
7. No duplicate object names.
8. All 7 Phase 2 tables are present.

Syntax/dependency validation path:

1. Static SQL parse/lint if available.
2. Optional local/shadow DB validation only (never production).
3. FK dependency order check.
4. Index/constraint order check.

`pgcrypto` condition check:

1. Verify target env already has `pgcrypto` or allows `CREATE EXTENSION`.
2. If not allowed, require approved platform/admin fallback.
3. Approval owner: security/platform.

Rollback/security checks:

1. Reverse dependency rollback order is valid.
2. Rollback drops only new objects.
3. Existing production truth/runtime data remains untouched.
4. Service-role-only initial access.
5. No public/family/mobile direct access.
6. No operator writes until workflow is approved.
7. No hidden manual truth.

Acceptance criteria:

1. All static checks pass.
2. Syntax/dependency validation path is approved.
3. `pgcrypto` condition is resolved.
4. Rollback checklist is accepted.
5. Security conditions are acknowledged.
6. Migration execution still requires explicit final approval.

## 21. Dry-run/static validation result

Status:

- **STATIC VALIDATION PASSED**
- Build sanity check passed: `rm -rf .next && npm run build`.

Key confirmations:

1. No `INSERT`/`UPDATE`/`DELETE`.
2. No `ALTER TABLE` against existing runtime/truth tables.
3. No `DROP`/`TRUNCATE`.
4. No `CREATE POLICY`/`GRANT`/`REVOKE`.
5. No `CREATE TRIGGER`/`CREATE FUNCTION`.
6. No dynamic SQL (`DO $$` / `EXECUTE`).
7. No county/fylke-specific logic.
8. All 7 Phase 2 tables are present.
9. FK references remain isolated to Phase 2 table set.
10. Active-row uniqueness uses `superseded_at IS NULL`.
11. Exact CHECK vocabularies pass.
12. Dependency/order checks pass.
13. Rollback comment order checks pass.
14. Naming checks pass (length/uniqueness/prefixes).
15. Security placeholder exists and no public/family/mobile grants are present.

Non-blocking warnings:

- Static validation does not replace target-environment `pgcrypto` permission/policy check.
- Static validation does not equal apply approval.

Gate status:

- Migration execution is **NOT approved** by this result.
- Next gate: **pgcrypto environment / platform permission check**.

## 22. pgcrypto environment check

Status:

- **PASSED**

Result:

- Query:
  - `select name, default_version, installed_version from pg_available_extensions where name = 'pgcrypto';`
- Returned:
  - `name = pgcrypto`
  - `default_version = 1.3`
  - `installed_version = 1.3`

Conclusion:

- `pgcrypto` is already installed/enabled in target Supabase environment.
- `gen_random_uuid()` should be available.
- `CREATE EXTENSION IF NOT EXISTS pgcrypto` in migration draft should be safe/no-op for this environment.

Gate status:

- Migration execution is **NOT approved** by this result.
- Next gate: **final controlled-apply approval**.

## 23. Controlled apply result

Status:

- **APPLIED TO TEST ENVIRONMENT**
- Target: `project_ref=egalvhjvdvmoqboxbwzo` / `my-app-test` main DB.
- Applied migration:
  - `supabase/migrations/20260506112154_school_identity_location_resolution_phase2.sql`

Pre-apply facts:

1. Git preflight was clean on `main`.
2. HEAD at apply precheck:
   - `1bd760984c1182098e22be6e0009d6513ee74120`
3. Migration SHA256 matched expected:
   - `bc72a924bcba216a2009662aee91d874772d7b1d694f51df9a9832ccdeaa4549`
4. `npx supabase migration list --linked` showed remote history did not contain `20260506112154`.
5. `npx supabase db push --linked --dry-run` planned only:
   - `20260506112154_school_identity_location_resolution_phase2.sql`

Apply facts:

1. Command executed:
   - `npx supabase db push --linked`
2. Applied migration:
   - `20260506112154_school_identity_location_resolution_phase2.sql`
3. Notice observed during apply:
   - `extension "pgcrypto" already exists, skipping`
4. Command finished successfully:
   - `Finished supabase db push.`

Post-apply facts:

1. Remote migration history includes:
   - `20260506112154`
2. All 7 Phase 2 tables exist:
   - `source_school_observations`
   - `school_identity_candidates`
   - `identity_aliases`
   - `school_locations`
   - `school_identity_resolution_decisions`
   - `programme_availability_publication_decisions`
   - `school_identity_review_events`
3. All 7 Phase 2 tables reported estimated row count `0`.
4. Indexes verified:
   - `uq_res_active_obs`
   - `uq_pub_active_obs_prog_stage`
5. Build sanity passed:
   - `rm -rf .next && npm run build`

Limitations during verification:

1. Direct `SELECT gen_random_uuid()` verification was not executed because `psql` was unavailable in the execution environment.
2. Full SQL introspection for RLS/grants was not fully executed because `supabase db dump --linked` required Docker in the execution environment.
3. Migration content review still confirms no `CREATE POLICY`, `GRANT`, or `REVOKE` statements; no manual SQL was executed.

Explicit scope boundary:

1. This result does **not** approve runtime/write integration.
2. This result does **not** enable operator workflow.
3. This result does **not** publish any PSA truth.
4. No backfill was executed.
5. No code changes were executed.
6. No migration file changes were executed.
7. No `db reset` was executed.

Next gate:

- **post-apply read-only smoke + Phase 2 integration planning**

## 24. Post-apply read-only smoke result

Status:

- **POST-APPLY SMOKE PASSED**
- Target: `project_ref=egalvhjvdvmoqboxbwzo` / `my-app-test`

Confirmations:

1. Migration history confirmed:
   - `Local=20260506112154`, `Remote=20260506112154`
2. All 7 Phase 2 tables exist:
   - `source_school_observations`
   - `school_identity_candidates`
   - `identity_aliases`
   - `school_locations`
   - `school_identity_resolution_decisions`
   - `programme_availability_publication_decisions`
   - `school_identity_review_events`
3. All 7 tables reported estimated row count `0`.
4. Indexes confirmed:
   - `uq_res_active_obs`
   - `uq_pub_active_obs_prog_stage`
5. Constraint/security/runtime static confirmations from migration file:
   - CHECK constraints present for `decision_state`, `publishability_state`, `alias_type`, and `verification_alignment_state`.
   - No `CREATE POLICY` / `GRANT` / `REVOKE`.
   - No `CREATE TRIGGER` / `CREATE FUNCTION`.
6. Runtime isolation confirmations:
   - No runtime/write integration.
   - No PSA publication changes.
7. Build sanity passed:
   - `rm -rf .next && npm run build`

Limitations:

1. Direct DB introspection for CHECK/RLS/grants was limited in this environment.
2. Verification relied on Supabase inspect outputs plus migration-file static confirmation for constraints/security clauses.

Gate implications:

1. Test apply cycle is **closed**.
2. Production/main rollout remains **NOT approved**.
3. Runtime/write integration remains **blocked**.

Main rollout status:

- **APPLIED TO MAIN SCHEMA**:
  - target `project_ref=bgmtxyfchtqjuvzuuoon`;
  - applied migration `20260506112154_school_identity_location_resolution_phase2.sql`.
- **MAIN POST-APPLY SMOKE PASSED**.
- **PHASE 2 SCHEMA ROLLOUT COMPLETE**.
- Runtime/write integration remains **blocked**.
- No backfill inserted.
- No data inserted.

Next gate:

- **owner decision: read-only integration planning or stop/freeze Phase 2 schema rollout as complete**
